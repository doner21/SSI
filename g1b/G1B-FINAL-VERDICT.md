# G1B — FINAL VERDICT: the salience falsifier fired (twice), thesis descoped

**Outcome:** **FAIL → DESCOPE per Open Decision #5.** The SSI-distinctive claim — that a *computed EIG/VoI context-conditioned belief-shift* salience beats a user-aware heuristic on abstention + usefulness + answer-changing VoI — is **falsified at the G1B synthetic pilot**, robustly, under a fair test. This is the FINAL authorized attempt; no further retries (FREEZE-v2 rule).

**Epistemic weight:** [GATE], pilot-grade **SYNTHETIC** (N=12, synthetic oracle). Does NOT establish/deny the *human* claim (owed at G5, N≥24). It DOES, per the plan's pre-registered investment-freeze, descope the EIG/VoI machinery out of the v1 heavy build.

---

## Two attempts, both fair-audited, same answer

| | v1 (frozen, later found partly unfair) | **v2 (fair: coupling + aligned EVSI + train-calib symmetry + S6 fix)** |
|---|---|---|
| SSI composite | 0.458 | **0.183** |
| SSI − generic linter | −0.174 | −0.421 |
| SSI − **user-aware linter** | −0.171 | **−0.333** |
| SSI − **own ablation (no EIG/VoI)** | −0.127 | **−0.521** |
| G1B-MV | FAIL (factored rejected) | FAIL (coupled also rejected; hierarchical favored) |

- **v2 cross-lab anti-rigging audit:** PASS-LOCK-READY, conf 0.98 — coupling applied identically to SSI *and* ablation, no oracle leak, train-only calibration behind an asserted wall, §2 world verbatim from v1, SSI could still win. It didn't.
- **The clean isolation held:** the ablation shares SSI's *entire* coupled belief model and differs only by removing EIG/VoI. The ablation beat SSI by 0.52 → **the EIG/VoI machinery is net-negative**, not merely unhelpful.
- **Two-sided failure:** v1 SSI over-asked (AQ below chance); v2 SSI under-asked (VoI=US=0). Tuned either way, the machinery does not beat simple heuristics.

## Why this is a win, not a defeat
Per plan §7: a successful build is *either* the imagined instrument, *or* the project learning cheaply and exactly why it couldn't — at a falsifier gate, before the heavy spend. We learned, for two synthetic cycles, that the distinctive salience moat does not survive its own cheap falsifier. Months of engine build were not spent on a thesis that fails on paper.

## What descope means (the remaining sub-choice is Donald's — Decision #5)
- **A1 — ship the user-aware heuristic Route 1.** The heuristic linter *won* here (it beat SSI). Reframe honestly as "smart warnings," NOT "computed belief-shift dialogue." Cheap to build; real but modest user value; not the distinctive moat.
- **A2 — Route-2-only.** Predict-and-build; the user is their own salience filter. Most honest/minimal; Route 1 becomes experimental/cut.
- Either way: **retract the "computed belief shift" claim** from v1 positioning; **lift the freeze** for the signal/engine stack on parts independent of Route 1.
- **Door not welded shut:** descope is the correct response to the *cheap* falsifier. The human claim is still formally owed at G5; if real-human evidence ever warrants, the distinctive claim can be revisited — but it is NOT built into v1 on the strength of a thesis that failed the cheap test.

## Record
- Attempt 1: PRE-REGISTRATION.md (afa823bc), artifacts/REPORT.md, artifacts/ROOT-CAUSE-V4PRO.md.
- Attempt 2: PRE-REGISTRATION-v2.md (c4042f91), HARNESS-SPEC-v2.md (706534f5), artifacts/ANTIRIG-AUDIT-V2-V4PRO.md, artifacts-v2/REPORT.md + results.json, FREEZE-v2.txt.
- Orchestration ledger: _orchestration/ATT_ORCHESTRATOR_ROLE_LEDGER.md.
