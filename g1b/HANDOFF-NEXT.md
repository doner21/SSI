# SSI DAW — RESUME HANDOFF (post-reboot continuation)

**You are the ORCHESTRATOR (piNen seat).** Do not execute the main task yourself; route to subagents via the deterministic `orchestrate` harness + cross-lab verifier. Generator≠certifier stays cross-lab (Opus authors; DeepSeek V4-Pro certifies).

## FIRST ACTIONS ON RESUME (in order)
1. Read this file fully.
2. Read `g1b/_orchestration/ATT_ORCHESTRATOR_ROLE_LEDGER.md` (full run history + role-integrity verdict).
3. Confirm Telegram is live: `telegram_pi status`; if needed `telegram_pi takeover`, then send Donald: "Back online after reboot, resuming SSI per HANDOFF-NEXT.md."
4. Then proceed with "Immediate autonomous work" below. Ask Donald only the two flagged decisions.

## WHERE THE PROJECT STANDS (settled)
- **G1B (salience falsifier): COMPLETE → DESCOPE ACCEPTED (Decision #5).** Across two pre-registered, cross-lab-audited attempts the SSI-distinctive thesis (computed EIG/VoI belief-shift salience beats a user-aware heuristic) was falsified. In v2 (fair test: coupling on SSI+ablation, aligned EVSI, train-calibration symmetry, S6 fixed) **SSI surfaced ZERO discretionary questions on held-out** — identical to silent-Route-2 — while the coupled-ablation (same belief model, no EIG/VoI gate) asked genuinely-pivotal questions (VoI=1 for 8/12 users). Mechanism: the EVSI gate self-silences on novel consequences it can't model. **The registered EIG/VoI machinery is CUT from the v1 build.** (Full evidence: `g1b/G1B-FINAL-VERDICT.md`, `g1b/G1B-LINTER-VS-SSI-ANALYSIS.md`.)
- **Investment-freeze on Route-1-specific work stands; it LIFTS for the engine/signal stack** on parts independent of Route 1.
- Strategic reframe: the surviving moat is **coherence-by-construction (G2/G3)** + **malleable regenerate-and-reload (G1A)**, both still unproven, both leaning hard on **Cmajor**. Full rationale: `NEXT-SCOPE-RECOMMENDATION.md` (project root).

## TWO DECISIONS FOR DONALD (ask via Telegram; do NOT pick for him)
- **D-A: Decision #5 sub-choice** — A1 (ship a cheap user-aware heuristic "smart warnings" Route 1, claim retracted) vs A2 (Route-2-only, leanest/most honest). *Low urgency:* both lift the engine-track freeze; this only decides whether a heuristic warnings layer is built later. Orchestrator light lean: **A2 for v1 focus**, heuristic warnings as an optional later add.
- **D-B: Decision #2 (Cmajor) host/target** — JUCE plugin host / `cmaj` standalone / bespoke C++. Needed to start G1A. Pair with the license/commercial read (below).

## IMMEDIATE AUTONOMOUS WORK (no Donald input required — start now)
Run these as orchestrator (route to subagents; keep the falsifier-first discipline, pre-register any gate before results):
1. **Cmajor license + host-viability decision brief** (research seat → e.g. `ssidaw-research`/internet-researcher, model deepseek-v4-pro or flash for grounding). Facts only, no commitment: commercial-agreement termination terms, Pro-tier pricing, GPLv3 implications for the business model, and which host integration (JUCE / cmaj / bespoke) makes the two-live-Performers concurrency spike buildable. Output: `g1b/../cmajor/CMAJOR-DECISION-BRIEF.md`. This gates D-B and the whole Cmajor bet.
2. **G0 — actor-lattice scope check** (paper, cheap): confirm every v1 corpus artifact resolves under the single-authority lattice; narrow plan language to "directional." Close the §3.3/G0 item. Output a short G0 verdict doc.
3. **Cmajor grounding dossier** (V4-Flash seat): real Cmajor API/syntax facts the eventual IR/authoring seats will need, so nothing is emitted from parametric memory.

## GATED NEXT (do NOT start until D-B host chosen + brief reviewed)
4. **G1A — reload-bridge concurrency spike** (the next falsifier). Narrow first-pass: can two live Engine/Performer instances be owned/advanced/fed/mixed concurrently in the chosen host at all? Pre-register the 4 reload metrics + concurrency proof; investment-freeze on fail → silence-boundary fallback. Minimal team: Architect(Opus) + V4-Pro + V4-Flash grounding.
5. After G1A clears → Phase 2 Coherent IR (G2), Phase 3 palette + manifest-truth (G3).

## SCOPE CUTS (locked by the falsification)
- CUT from v1 heavy build: Phase-5 real salience engine, EIG/VoI computation, Route-1 dialogic machinery, old central gate G5.
- KEEP cheaply: fossil/intent store (L0/L2) for **regeneration biasing** only (personal-dialect priors), NOT salience dialogue.
- Central gate is now **G1A → G2/G3**, not G5.

## RESUME MECHANICS (how you got here)
- The machine was rebooted intentionally (it had been up a long time / slow). On logon, `agent/pi-ssi-autostart.bat` (also in the Windows Startup folder) launches Pi in `C:/Users/doner/ssi`. A `agent_scheduler` wake-up (~5 min post-reboot) re-injects the resume prompt. If auto-resume did NOT fire, this file IS the handoff — just continue from "FIRST ACTIONS ON RESUME."
- **Dependency caveat:** resume requires the machine to reach the Windows desktop. If it sits at a locked login screen, the Startup script (and Pi) will not run until Donald logs in.
